# Soulbase Gateway 配置文件
# 复制此文件为 .env 并根据实际情况修改

# ==========================================
# Gateway 核心配置
# ==========================================

# 配置文件路径（相对于项目根目录）
# 作用：指定gateway的TOML配置文件位置
# 默认：config/gateway.local.toml（本地开发环境）
GATEWAY_CONFIG_FILE=config/gateway.local.toml

# ==========================================
# 认证配置
# ==========================================

# Bearer Token（用于API认证）
# 作用：客户端调用API时使用的Bearer Token
# 使用方式：Authorization: Bearer ${GATEWAY_TOKEN_LOCAL}
# 对应配置：gateway.local.toml中的auth.tokens[0].token_env
# 本地开发建议值：dev-token
GATEWAY_TOKEN_LOCAL=dev-token

# HMAC签名密钥（用于HMAC认证）
# 作用：用于HMAC签名验证，提供更高级别的安全性
# 使用场景：自动化脚本、服务间调用
# 对应配置：gateway.local.toml中的auth.hmac_keys[0].secret_env
# 本地开发建议值：dev-hmac
GATEWAY_HMAC_LOCAL=dev-hmac

# ==========================================
# 可选配置（生产环境）
# ==========================================

# 如需连接真实的LLM Provider，取消注释并配置以下选项：

# OpenAI配置
# OPENAI_API_KEY=sk-xxx...
# OPENAI_MODEL=gpt-4o-mini

# Claude配置
# ANTHROPIC_API_KEY=sk-ant-xxx...
# CLAUDE_MODEL=claude-3-haiku

# Gemini配置
# GEMINI_API_KEY=xxx...
# GEMINI_MODEL=gemini-pro

# SurrealDB配置（如需持久化）
# SURREAL_URL=ws://127.0.0.1:8000
# SURREAL_USERNAME=root
# SURREAL_PASSWORD=root
# SURREAL_NAMESPACE=soul
# SURREAL_DATABASE=base

# Redis配置（如需缓存）
# REDIS_URL=redis://127.0.0.1:6379

# ==========================================
# 运行模式说明
# ==========================================
# 1. 本地开发模式（当前配置）
#    - 使用内存存储（memory）
#    - 使用本地echo模拟LLM
#    - 适合快速开发和测试
#
# 2. 真实LLM模式
#    - 配置真实的LLM API Key
#    - 其他服务仍使用内存存储
#    - 适合功能验证
#
# 3. 生产模式
#    - 配置所有真实服务
#    - 使用SurrealDB持久化
#    - 使用Redis缓存
#    - 适合生产部署
